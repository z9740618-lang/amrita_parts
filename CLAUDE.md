# amrita_parts：自動作業のルール

puppet リポジトリ（`z9740618-lang/puppet`）との間で、Issue を使った自動のやりとりをする。そのときのルール。

## 自動でやっていいこと
- 相手のリポジトリに Issue を立てる。Issue を読んで作業する。作業ブランチに push する。PR を作る。
- 自分の持ち場のファイルだけを変更する。
  - amrita_parts：パーツ画像（と、それを作るスクリプト・manifest・検査画像・作業記録）
  - puppet：コード。amrita_parts 側からは変更しない

## Rez の承認が要ること
Issue か PR にコメントして、承認を待つ。承認されるまで進めない。
- main へのマージ
- 新しい分割計画を立てること。パーツの ID や構成（パーツの追加・削除・分け方・描画順）を変えること
- 見た目やデザインの判断（眉の形、表情の作り方など）

## 往復を止めるルール
- 「普段の動き」で見えない問題は Issue にしない。
  - 普段の動きの定義は、puppet の `samples/README.md`（「残っている問題の仕分け」の節）に従う。
- 同じ問題で3往復したら、自動で続けない。Rez に判断を仰ぐ。

## 報告
- 1日の終わりに、その日に動いた Issue と PR を1つのコメントにまとめて書く。
